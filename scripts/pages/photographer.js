//Mettre le code JavaScript lié à la page photographer.html

const photographerPage = {
    getPhotographer:async function () {
        const photographerPage = document.querySelector(".photograph-main");
        console.log(photographerPage);

    },

    displayData:async function(photographers) {
        // const photographerPage = document.querySelector(".photographe-main");
        // console.log(photographerPage);

        // photographers.find((photographer) => {
        //     const photographerModel = photographerFactory(photographer);
        //     const userCardDOM = photographerModel.getUserCardDOM();
        //     photographerPage.appendChild(userCardDOM);
        // });
    },

}